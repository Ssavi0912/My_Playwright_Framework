// We are creating a variable named tool and storing the text "Playwright" in it.
let tool="Playwright"

// This is a template literal, written using backticks ` ` instead of normal quotes.
// Wherever we write ${tool}, JavaScript replaces it with the actual value stored in the tool variable.
// This lets us build a sentence using the variable value without manually joining strings with +.
console.log(`We will be using ${tool} , ${tool} have different components`);
